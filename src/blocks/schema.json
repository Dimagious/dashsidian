{
  "version": "1.3.0",
  "blocks": {
    "tiles": {
      "summary": "A grid of link tiles for navigating the vault.",
      "root": {
        "columns": {
          "type": "number",
          "default": 4,
          "doc": "columns in the grid, 1 to 8"
        },
        "items": {
          "type": "list",
          "required": true,
          "doc": "the list of tiles"
        }
      },
      "item": {
        "label": {
          "type": "string",
          "required": true,
          "doc": "caption",
          "aliases": [
            "title",
            "name"
          ]
        },
        "path": {
          "type": "string",
          "required": true,
          "doc": "where it leads; understands File.base#View"
        },
        "icon": {
          "type": "string",
          "doc": "emoji",
          "aliases": [
            "emoji"
          ]
        },
        "sub": {
          "type": "string",
          "doc": "small caption under the title"
        },
        "badge": {
          "type": "string|number",
          "doc": "count, meaning the number of notes in the folder, or your own string"
        },
        "accent": {
          "type": "boolean",
          "doc": "accent stripe on the left"
        }
      },
      "example": "columns: 4\nitems:\n  - { label: Inbox, path: 00-Inbox, icon: 📥, badge: count }\n  - { label: Sport, path: 01-Areas/Sport/Training-Log, icon: 🏆, sub: workouts, accent: true }"
    },
    "stats": {
      "summary": "Number cards: one value per card, computed over a selection of notes.",
      "root": {
        "columns": {
          "type": "number",
          "default": 3,
          "doc": "columns in the grid, 1 to 6"
        },
        "items": {
          "type": "list",
          "required": true,
          "doc": "the list of cards"
        }
      },
      "item": {
        "label": {
          "type": "string",
          "required": true,
          "doc": "caption under the number",
          "aliases": [
            "title",
            "name"
          ]
        },
        "source": {
          "type": "string",
          "doc": "folder; includes nested ones",
          "aliases": [
            "folder",
            "from"
          ]
        },
        "tag": {
          "type": "string",
          "doc": "tag, with or without the hash"
        },
        "where": {
          "type": "string",
          "doc": "a single condition like `year = 2026`, `rating >= 4`, `tags contains books`; `and`/`or` are not supported"
        },
        "field": {
          "type": "string",
          "doc": "numeric frontmatter property; required for everything but count and streak",
          "aliases": [
            "property",
            "prop"
          ]
        },
        "agg": {
          "type": "string",
          "default": "count",
          "doc": "count sum avg min max latest streak",
          "aliases": [
            "aggregate"
          ]
        },
        "unit": {
          "type": "string",
          "doc": "a suffix after the number: km, %, d."
        },
        "precision": {
          "type": "number",
          "doc": "decimal places, 0 to 6; by default a whole number stays whole and a fraction gets one decimal"
        },
        "trend": {
          "type": "string|number",
          "doc": "sketch the last N days ending today: `30d`. Needs `field`"
        },
        "icon": {
          "type": "string",
          "doc": "emoji",
          "aliases": [
            "emoji"
          ]
        },
        "sub": {
          "type": "string",
          "doc": "small caption under the title"
        }
      },
      "example": "columns: 3\nitems:\n  - { label: Notes, source: 01-Areas, agg: count }\n  - { label: Sleep, source: Diary, field: sleep_score, agg: avg, precision: 1, trend: 30d }\n  - { label: Days in a row, source: Diary, field: sleep_score, agg: streak, unit: d. }",
      "notes": [
        "`streak` counts the longest run of consecutive days and `latest` takes the value from the newest note. Both need YYYY-MM-DD note names.",
        "Nothing to count, and the card shows a dash rather than a zero: \"no data\" and \"zero\" are different answers.",
        "`trend` sketches the days of a trailing window ending today, scaled between the smallest and largest value in that window rather than from zero. A day without a note is left out, not drawn as a zero."
      ]
    },
    "progress": {
      "summary": "Bars towards a goal: how far a number has come against a target.",
      "root": {
        "items": {
          "type": "list",
          "required": true,
          "doc": "the list of bars"
        }
      },
      "item": {
        "label": {
          "type": "string",
          "required": true,
          "doc": "caption above the bar",
          "aliases": [
            "title",
            "name"
          ]
        },
        "goal": {
          "type": "number",
          "required": true,
          "doc": "the target to fill towards; must be above zero",
          "aliases": [
            "target"
          ]
        },
        "source": {
          "type": "string",
          "doc": "folder; includes nested ones",
          "aliases": [
            "folder",
            "from"
          ]
        },
        "tag": {
          "type": "string",
          "doc": "tag, with or without the hash"
        },
        "where": {
          "type": "string",
          "doc": "a single condition like `year = 2026`, `rating >= 4`, `tags contains books`; `and`/`or` are not supported"
        },
        "field": {
          "type": "string",
          "doc": "numeric frontmatter property; required for everything but count and streak",
          "aliases": [
            "property",
            "prop"
          ]
        },
        "agg": {
          "type": "string",
          "default": "count",
          "doc": "count sum avg min max latest streak",
          "aliases": [
            "aggregate"
          ]
        },
        "unit": {
          "type": "string",
          "doc": "a suffix after the numbers: km, %, d."
        },
        "precision": {
          "type": "number",
          "doc": "decimal places, 0 to 6; by default a whole number stays whole and a fraction gets one decimal"
        },
        "icon": {
          "type": "string",
          "doc": "emoji",
          "aliases": [
            "emoji"
          ]
        },
        "sub": {
          "type": "string",
          "doc": "small caption under the bar"
        }
      },
      "example": "items:\n  - { label: Books this year, source: Books, where: \"year = 2026\", agg: count, goal: 50 }\n  - { label: Running volume, source: Diary, field: distance_km, agg: sum, goal: 200, unit: km }",
      "notes": [
        "Going past the goal is shown as it is, 125% stays 125%; only the bar itself stops at full.",
        "Nothing to count, and the bar stays empty and the value shows a dash rather than a zero."
      ]
    },
    "today": {
      "summary": "A day row: today's date and links to the daily, weekly and monthly notes.",
      "root": {
        "daily": {
          "type": "boolean",
          "default": true,
          "doc": "link to the daily note"
        },
        "weekly": {
          "type": "boolean",
          "default": false,
          "doc": "link to the weekly note"
        },
        "monthly": {
          "type": "boolean",
          "default": false,
          "doc": "link to the monthly note"
        },
        "title": {
          "type": "string",
          "doc": "a custom heading instead of today's date"
        }
      },
      "example": "daily: true\nweekly: true\nmonthly: true",
      "notes": [
        "Takes the folder and the name format from the Periodic Notes plugin when installed; for the day it also picks up the core \"Daily notes\". A folder set in the Dashy settings wins over both.",
        "With none of the keys given, only the daily note is shown. With at least one given, exactly those apply.",
        "If the note does not exist yet the link is still drawn, dimmed: clicking it creates the note."
      ]
    },
    "countdown": {
      "summary": "Cards counting the days to a date.",
      "root": {
        "columns": {
          "type": "number",
          "default": 3,
          "doc": "columns in the grid, 1 to 6"
        },
        "items": {
          "type": "list",
          "required": true,
          "doc": "the list of dates"
        }
      },
      "item": {
        "label": {
          "type": "string",
          "required": true,
          "doc": "what the date is",
          "aliases": [
            "title",
            "name"
          ]
        },
        "date": {
          "type": "string",
          "required": true,
          "doc": "the date itself, YYYY-MM-DD"
        },
        "icon": {
          "type": "string",
          "doc": "emoji",
          "aliases": [
            "emoji"
          ]
        },
        "sub": {
          "type": "string",
          "doc": "small caption under the label"
        }
      },
      "example": "columns: 3\nitems:\n  - { label: IRONMAN 70.3, date: 2026-11-15, icon: 🏊 }\n  - { label: Holiday, date: 2026-12-20, icon: 🏖, sub: two weeks off }",
      "notes": [
        "A date that has passed is shown too, counting up instead of down. What happened yesterday is still worth seeing.",
        "The day count is whole days, so a daylight saving switch cannot shift it."
      ]
    },
    "heatmap": {
      "summary": "A year by days: one cell per day, coloured by a number from frontmatter.",
      "root": {
        "source": {
          "type": "string",
          "doc": "folder; includes nested ones",
          "aliases": [
            "folder",
            "from"
          ]
        },
        "tag": {
          "type": "string",
          "doc": "tag, with or without the hash"
        },
        "where": {
          "type": "string",
          "doc": "a single condition like `year = 2026`, `rating >= 4`, `tags contains books`; `and`/`or` are not supported"
        },
        "field": {
          "type": "string",
          "required": true,
          "doc": "numeric frontmatter property",
          "aliases": [
            "property",
            "prop"
          ]
        },
        "color": {
          "type": "string",
          "default": "blue",
          "doc": "blue green cyan purple pink orange red gray, or #rrggbb",
          "aliases": [
            "colour"
          ]
        },
        "bands": {
          "type": "list",
          "doc": "thresholds from the top down: [90, 80, 60] or [{min, alpha, label}]; anything below the lowest falls into the bottom band"
        },
        "link": {
          "type": "boolean",
          "default": true,
          "doc": "clicking a cell opens that day's note"
        },
        "title": {
          "type": "string",
          "doc": "a custom heading instead of the automatic one"
        }
      },
      "example": "source: 01-Areas/Personal/Diary\nfield: sleep_score\ncolor: purple\nbands: [90, 80, 60]",
      "notes": [
        "Notes must be named as YYYY-MM-DD dates. That is how the block knows which cell they belong to.",
        "Years are taken from the data: once a new year appears, a second grid is drawn."
      ]
    }
  },
  "notInV1": {
    "table": "That is Obsidian Bases. Link to a base view from a tile instead: `path: Vault.base#My view`.",
    "chart": "That is the Obsidian Charts plugin. Dashy does not draw charts.",
    "kanban": "That is the Kanban plugin.",
    "tasks": "That are the Tasks or Dataview plugins."
  }
}
