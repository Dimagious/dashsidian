{
  "version": "0.2.0",
  "blocks": {
    "tiles": {
      "summary": "Сетка плиток-ссылок для навигации по хранилищу.",
      "root": {
        "columns": { "type": "number", "default": 4, "doc": "колонок в сетке, 1–8" },
        "items": { "type": "list", "required": true, "doc": "список плиток" }
      },
      "item": {
        "label": { "type": "string", "required": true, "doc": "подпись", "aliases": ["title", "name"] },
        "path": { "type": "string", "required": true, "doc": "куда ведёт; понимает Файл.base#Вид" },
        "icon": { "type": "string", "doc": "эмодзи", "aliases": ["emoji"] },
        "sub": { "type": "string", "doc": "мелкая подпись под названием" },
        "badge": { "type": "string|number", "doc": "count — число заметок в папке, либо своя строка" },
        "accent": { "type": "boolean", "doc": "акцентная полоса слева" }
      },
      "example": "columns: 4\nitems:\n  - { label: Inbox, path: 00-Inbox, icon: 📥, badge: count }\n  - { label: Sport, path: 01-Areas/Sport/Training-Log, icon: 🏆, sub: тренировки, accent: true }"
    },
    "stats": {
      "summary": "Карточки чисел: на каждой одно значение, посчитанное по выборке заметок.",
      "root": {
        "columns": { "type": "number", "default": 3, "doc": "колонок в сетке, 1–6" },
        "items": { "type": "list", "required": true, "doc": "список карточек" }
      },
      "item": {
        "label": { "type": "string", "required": true, "doc": "подпись под числом", "aliases": ["title", "name"] },
        "source": { "type": "string", "doc": "папка; включает вложенные", "aliases": ["folder", "from"] },
        "tag": { "type": "string", "doc": "тег, с решёткой или без" },
        "where": { "type": "string", "doc": "условие вида `year = 2026`, `rating >= 4`, `tags contains книги`" },
        "field": { "type": "string", "doc": "числовое поле frontmatter; обязательно для всего, кроме count и streak", "aliases": ["property", "prop"] },
        "agg": { "type": "string", "default": "count", "doc": "count sum avg min max latest streak", "aliases": ["aggregate"] },
        "unit": { "type": "string", "doc": "приписка после числа: км, %, дн." },
        "precision": { "type": "number", "doc": "знаков после запятой, 0–6; по умолчанию целое как есть, дробное до одного знака" },
        "icon": { "type": "string", "doc": "эмодзи", "aliases": ["emoji"] },
        "sub": { "type": "string", "doc": "мелкая подпись под названием" }
      },
      "example": "columns: 3\nitems:\n  - { label: Заметок, source: 01-Areas, agg: count }\n  - { label: Сон, source: Дневник, field: sleep_score, agg: avg, precision: 1 }\n  - { label: Без пропусков, source: Дневник, field: sleep_score, agg: streak, unit: дн. }",
      "notes": [
        "`streak` считает самую длинную цепочку подряд идущих дней, `latest` берёт значение из самой поздней заметки — обоим нужны имена-даты YYYY-MM-DD.",
        "Считать нечего — карточка показывает прочерк, а не ноль: «нет данных» и «ноль» это разные ответы."
      ]
    },
    "heatmap": {
      "summary": "Год по дням: клетка на каждый день, цвет по числу из frontmatter.",
      "root": {
        "source": { "type": "string", "doc": "папка; включает вложенные", "aliases": ["folder", "from"] },
        "tag": { "type": "string", "doc": "тег, с решёткой или без" },
        "where": { "type": "string", "doc": "условие вида `year = 2026`, `rating >= 4`, `tags contains книги`" },
        "field": { "type": "string", "required": true, "doc": "числовое поле frontmatter", "aliases": ["property", "prop"] },
        "color": { "type": "string", "default": "blue", "doc": "blue green cyan purple pink orange red gray, либо #rrggbb", "aliases": ["colour"] },
        "bands": { "type": "list", "doc": "пороги сверху вниз: [90, 80, 60] либо [{min, alpha, label}]" },
        "link": { "type": "boolean", "default": true, "doc": "клик по клетке открывает заметку дня" },
        "title": { "type": "string", "doc": "свой заголовок вместо автоматического" }
      },
      "example": "source: 01-Areas/Personal/Дневник\nfield: sleep_score\ncolor: purple\nbands: [90, 80, 60]",
      "notes": [
        "Заметки должны называться датой YYYY-MM-DD — так блок понимает, в какую клетку их класть.",
        "Годы определяются по данным: появится следующий год — нарисуется второй сеткой."
      ]
    }
  },
  "notInV1": {
    "таблица": "Это Obsidian Bases. Сошлись на вид базы из плитки: `path: Vault.base#Мой вид`.",
    "график": "Это плагин Obsidian Charts. Dashy графики не рисует.",
    "канбан": "Это плагин Kanban.",
    "задачи": "Это плагины Tasks или Dataview."
  }
}
