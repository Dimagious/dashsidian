# Tiles narrowed by a selection (B-114)

A separate note, so the tile count and order the other specs assert on
`Dashboard.md` stay untouched. `Diary` has seven notes with `gym: true`
out of ten.

```tiles
columns: 1
items:
  - { label: Gym, path: Diary, where: "gym = true", badge: count }
```
